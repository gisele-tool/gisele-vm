parser:
  gvm.citrus
rules:
  gvm:
    - [ block+ ]
  block:
    - [ label, instruction+ ]
  label:
    - '::Integer'
    - '::Symbol'
  instruction:
    # generic
    - send
    - invoke
    - get
    - set
    # lifecycle
    - puid
    - fetch
    - fork
    - cont
    - then
    - schedule
    - unschedule
    - wait
    - save
    # events
    - event
    # stack
    - pop
    - push
    - group
    # to clean
    - notify
    - parent
    - resume
  # generic
  send:
    - [ 'symbol_arg?' ]
  invoke:
    - [ 'symbol_arg?' ]
  get:
    - [ 'symbol_arg?' ]
  set:
    - [ 'symbol_arg?' ]
  # lifecycle
  puid:
    - []
  fetch:
    - [ 'argument?' ]
  fork:
    - [ 'label_arg?' ]
  cont:
    - [ 'label_arg?' ]
  then:
    - [ 'label_arg?' ]
  schedule:
    - []
  unschedule:
    - []
  wait:
    - [ 'integer_arg?' ]
  save:
    - [ 'integer_arg?' ]
  # stack
  pop:
    - [ 'integer_arg?' ]
  push:
    - [ argument ]
  group:
    - [ 'integer_arg?' ]
  # events
  event:
    - [ 'symbol_arg?' ]
  # to clean
  notify:
    - []
  parent:
    - []
  resume:
    - []
  argument:
    - symbol_arg
    - integer_arg
    - string_arg
    - module_arg
  label_arg:
    - symbol_arg
    - integer_arg
  symbol_arg:  '::Symbol'
  integer_arg: '::Integer'
  string_arg:  '::String'
  module_arg:  '::Module'
